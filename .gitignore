# Dependencias y builds
node_modules/
dist/
.next/
out/
build/
coverage/

# Entorno y secretos (nunca subir .env reales)
.env
.env.local
.env.*.local
**/.env
!**/.env.example

# Logs y SO
*.log
.DS_Store
Thumbs.db

# Reporte generado por audit:production (cPanel)
production-db-audit.txt
api/production-db-audit.txt

# IDE / agentes locales
.idea/
.vscode/
.agents/
.claude/
.windsurf/
skills-lock.json

# TypeScript / Vercel
*.tsbuildinfo
.vercel/

# Prisma generado (se regenera con prisma generate)
api/src/generated/
**/generated/prisma/

# Paquetes de deploy locales (subir a cPanel, no a Git)
studio-s-api-production-*.zip

# Este repositorio publica SOLO el frontend (web/).
# La API se mantiene local / se despliega por cPanel, no en GitHub.
/api/
/docs/
studio-s-api-production-*.zip
